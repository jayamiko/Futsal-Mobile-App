import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Home = () => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text
                style={{ flex: 1, alignItems: 'center', color: 'black' }}
            >Home Screen</Text>
        </View>
    )
}

export default Home;

const styles = StyleSheet.create({});